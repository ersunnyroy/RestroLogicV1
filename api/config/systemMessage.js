const error_message = {
    INTERNAL_ERROR: 'Something went wrong, Please try again',
    ALL_FIELDS_REQUIRED: 'All fields are required',
}

const success = {
    CREATED: 'Record created successfully',
}

module.exports = { error_message, success }
